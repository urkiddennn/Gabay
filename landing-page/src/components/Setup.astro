---
// Setup.astro
---

<section
    id="setup"
    class="w-full max-w-7xl mx-auto px-6 py-12 border-t border-border-main/50"
>
    <div class="text-center mb-16">
        <h2
            class="text-3xl md:text-5xl font-bold text-text-main mb-6 uppercase tracking-tight"
        >
            GET STARTED IN <span class="text-blue-500">SECONDS</span>
        </h2>
        <p
            class="text-text-muted text-lg max-w-2xl mx-auto leading-relaxed font-medium"
        >
            Starting with Gabay is as easy as sending a message to a friend.
        </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <!-- Step 1: Add Bot -->
        <div
            class="flex flex-col gap-4 p-8 bg-bg-surface border border-border-main rounded-lg"
        >
            <div
                class="w-8 h-8 rounded-full bg-text-main text-bg-page flex items-center justify-center font-bold text-sm"
            >
                1
            </div>
            <h3 class="text-xl font-bold text-text-main">Add to Telegram</h3>
            <p class="text-sm text-text-muted leading-relaxed">
                Just search for <strong>@GabayAgentBot</strong> on Telegram or click
                the button above to start your first chat.
            </p>
            <div
                class="mt-auto pt-4 flex items-center gap-2 text-xs text-blue-500 font-semibold"
            >
                No installation required
            </div>
        </div>

        <!-- Step 2: Link Accounts -->
        <div
            class="flex flex-col gap-4 p-8 bg-bg-surface border border-border-main rounded-lg"
        >
            <div
                class="w-8 h-8 rounded-full bg-text-main text-bg-page flex items-center justify-center font-bold text-sm"
            >
                2
            </div>
            <h3 class="text-xl font-bold text-text-main">Connect Workspace</h3>
            <p class="text-sm text-text-muted leading-relaxed">
                Click the "Connect" link inside the chat to safely link your
                Google and Notion accounts.
            </p>
            <div
                class="mt-auto pt-4 flex items-center gap-2 text-xs text-emerald-500 font-semibold"
            >
                Secure & Official pairing
            </div>
        </div>

        <!-- Step 3: Ask Away -->
        <div
            class="flex flex-col gap-4 p-8 bg-bg-surface border border-border-main rounded-lg"
        >
            <div
                class="w-8 h-8 rounded-full bg-text-main text-bg-page flex items-center justify-center font-bold text-sm"
            >
                3
            </div>
            <h3 class="text-xl font-bold text-text-main">Start Chatting</h3>
            <p class="text-sm text-text-muted leading-relaxed">
                That's it! Just tell Gabay what you need—like finding an old
                email or writing a report.
            </p>
            <a
                href="/docs"
                class="mt-auto pt-4 text-text-main font-bold text-xs uppercase hover:underline"
                >See common tasks →</a
            >
        </div>
    </div>
</section>
